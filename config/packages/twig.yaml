twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes:
      - bootstrap_3_layout.html.twig
      - _formTheme.html.twig
    globals:
        pathToWeb: "%kernel.root_dir%/../"

