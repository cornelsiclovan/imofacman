{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>New Activity</h1>

                {{ form_start(activityForm) }}
                    {{ form_widget(activityForm) }}
                    <button type = "submit" class="btn btn-action">Adauga activitate</button>
                {{ form_end(activityForm) }}
            </div>
        </div>
    </div>
{% endblock %}