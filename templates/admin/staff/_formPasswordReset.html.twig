{{ form_start(staffPasswordChangeForm) }}
    {{ form_row(staffPasswordChangeForm.plainPassword.first,{'label' : 'Password'}) }}
    {{ form_row(staffPasswordChangeForm.plainPassword.second, {'label' : 'Repetă parola'}) }}

<button type = "submit" class="btn btn-action" formnovalidate>{{ buttonText }}</button>
{{ form_end(staffPasswordChangeForm) }}