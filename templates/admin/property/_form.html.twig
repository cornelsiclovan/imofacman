{{ form_start(propertyForm) }}
    {{ form_row(propertyForm.name, {'label' : 'Denumire proprietate'}) }}
    {{ form_row(propertyForm.address ,{'label': 'Adresa proprietate'}) }}
    {{ form_row(propertyForm.owner, {'label' : 'Selecteaza un proprietar'}) }}

<button type = "submit" class="btn btn-action" formnovalidate>{{ buttonText }}</button>
{{ form_end(propertyForm) }}